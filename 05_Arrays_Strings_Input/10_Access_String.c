#include <stdio.h>
int main(){
    char myString[] = "Hello World!";
    printf("%c", myString[0] ); //Printing first character of the string---one character thats why format specifier %c
    return 0;
}