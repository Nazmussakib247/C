#include <stdio.h>

int main (){
    // Declaring more than one variable of the same type
    int a = 20, b = 39, c = 87, d = 273, e = 2737, f = 200;
    printf("%d\n", a + b + c + d + e + f);
//Assiging same value to multiple variable
    int x , y, z;
    x = y = z = 29;
    printf("%d", x + y + z);
return 0;
} 