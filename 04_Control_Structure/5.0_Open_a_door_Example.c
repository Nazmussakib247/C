# include <stdio.h>
int main (){
    int doorCode = 29392;
    if (doorCode == 1000) {
        printf("Door is open");
    } else {
        printf("Door is locked, cant open, Enter right code again");
    }
    return 0;
}